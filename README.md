# Exam_secure

FRONTEND:
1. Navigate to /frontend from the terminal
2. Run the command 'ng serve' or 'npm run ng serve'

BACKEND:
1. Navigate to /backend from another terminal
2. Run the command 'uvicorn main:app --reload --port 8000